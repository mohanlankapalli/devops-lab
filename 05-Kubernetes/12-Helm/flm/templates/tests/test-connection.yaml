apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flm.fullname" . }}-test-connection"
  labels:
    {{- include "flm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
